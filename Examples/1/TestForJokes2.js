//Test for jokes
var joke = JavaScriptWalkedIntoABar
var toldJoke = false
var $punchline =
 "Better watch out for those semi-colons."
var entage = 20;
var result
if (toldJoke == true) {
 Alert($punchline);
} else
 alert(joke);
